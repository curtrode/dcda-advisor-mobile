# Microsoft Azure AD Configuration (for OneDrive integration)
# Get these from Azure Portal > App registrations
VITE_AZURE_CLIENT_ID=your_azure_app_client_id
VITE_AZURE_TENANT_ID=your_tcu_tenant_id
# Optional: Custom redirect URI (defaults to current origin)
# VITE_AZURE_REDIRECT_URI=https://your-app.example.com
